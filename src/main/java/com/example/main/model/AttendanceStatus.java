package com.example.main.model;

/**
 * Enum to define the possible attendance statuses for a student in a lecture.
 */
public enum AttendanceStatus {
	   PRESENT,
	    ABSENT,
	    LEAVE // For approved leave
}
